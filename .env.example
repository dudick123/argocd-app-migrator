# ArgoCD App Migrator - Environment Variables Template
# Copy to .env and fill in values (NEVER commit .env to git)

# Example: Default output directory (optional)
# OUTPUT_DIR=/path/to/output

# Example: Log level (optional)
# LOG_LEVEL=INFO
